# Marketplace Documentation Portal

Open `index.html` in a browser to use the static documentation portal.

## Usage

- Use the top quick links for Requirements, Use Cases, User Stories, Business Rules, and Traceability Matrix.
- Use the search field to filter modules or file names.
- Expand a module card to open module-specific `USE_CASES_*.md` and `USER_STORIES_*.md` docs.
